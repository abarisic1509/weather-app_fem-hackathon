export type City = {
	name: string;
	lat: string;
	lng: string;
	country: string;
};
export type CitiesData = {
	cities: City[];
};

export type CityLocation = {
	latitude: string;
	longitude: string;
};
